{
  "name": "the-forge",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "install": "npm install"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=18.0.0"
  }
}
